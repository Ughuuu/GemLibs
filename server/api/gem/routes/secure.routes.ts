"use strict";

import * as express from 'express';

export class SecureRoutes {
  static init(app: express.Application, isAuthenticated : any) {
    //app.get('/home', isAuthenticated, function(req, res){
    //});
  }
};
