export class Plugin {
    constructor(
        public name: string,
        public content: string,
        public version: string,
        public createdAt: Date,
        public username: string) { }
}